# Ambiente virtual e caches (são reinstalados no build)
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Git e metadados
.git/
.gitignore
.gitattributes

# Segredos (injetados em runtime via env_file ou volume — nunca dentro da imagem)
.env
.streamlit/secrets.toml

# Dados gerados em runtime (montados como volume)
data/
logs/
exports/
*.db
*.log

# Docs / docker (não precisam ir para a imagem)
README.md
